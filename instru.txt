你好，Claude。我是一个新手，正在做一个课程设计。我目前在 Cursor 的终端里，当前文件夹下只有我这次课设所需的“小型数据集”和一个介绍文档。

官方实验的 Baseline 是：https://github.com/facebookresearch/InterHand2.6M.git
特别说明：我的电脑配备了 8GB 的独立显卡，支持 CUDA 加速。

根据我的课设 Readme 文档要求，我们需要用我手头的小型数据集来复现这个实验。请你作为资深的 AI 编程专家，严格按照以下步骤帮我全自动执行，遇到每一步的命令行执行或文件修改时，请提示我按回车（Enter）批准：

【第一步：克隆官方代码与对齐路径】
1. 请直接在当前目录下运行 git clone 命令，把官方代码库克隆下来。
2. 官方代码默认的数据集根目录名字可能不同，请帮我检查并确保我的数据集根目录命名为 `InterHand2.6M_5fps`。
3. 请帮我查看官方代码期望的数据集存放路径（通常在 data/ 目录下或通过 config 配置文件指定），并自动把我本地的这个 `InterHand2.6M_5fps` 数据集文件夹（包含其中的 images 和 annotation）移动到官方代码要求的正确位置。

【第二步：根据课设私设，修改数据加载逻辑（核心）】
我手里的标签文件（annotation）是官方完整的，但图片（images）被大幅删减了：训练集（train）只保留了 Capture0 的图片；测试集（test）只保留了 Capture2 的图片。如果直接运行官方代码会因为找不到其他图片而报错崩溃。
1. 请帮我找到官方代码中负责初始化加载数据（Dataloader 或 Dataset 初始化循环）的 Python 脚本。
2. 请在循环加载数据的逻辑里，帮我添加以下判断条件：
   - 如果当前是训练集（train），判断数据的 Capture_id 是否为 0，若是则继续加载，否则跳过（skip）。
   - 如果当前是测试集（test），判断数据的 Capture_id 是否为 2，若是则继续加载，否则跳过（skip）。
3. 修改完成后，请帮我检查或加上一行代码：在数据加载完毕后，自动 `print` 出最终成功加载的图片数量，以便我验证是否正确加载了“训练集约 10 万张、测试集约 2 万张”的目标。
4. 请在终端告诉我你具体修改了哪个文件的哪几行代码。

【第三步：配置虚拟环境与显卡依赖安装】
1. 请在当前目录下帮我创建并激活一个 Python 虚拟环境。
2. 帮我阅读项目中的环境说明，自动安装好所有需要的依赖库。
3. 请务必帮我安装兼容我显卡的 PyTorch CUDA 版本（确保能调用我的 8GB 独显）。如果遇到旧版本库在当前系统下不兼容的报错，请你自动尝试更换版本或修改参数来解决，直到环境完全配置成功。

【第四步：配置显存优化并一键启动实验】
1. 环境和代码修改完毕后，请帮我找到运行该项目的启动命令（通常在 main 目录下）。
2. 请帮我检查代码中的 `batch_size`（批大小）设置。因为我手里只有 8GB 显存，如果官方默认的 batch_size 太大（比如 64 或 128），请帮我适度调小（例如调成 16 或 32），并开启显存优化，防止跑实验时发生 `Out of Memory (OOM)` 显存溢出错误。
3. 在虚拟环境下正式帮我把这个实验（或者测试评估脚本）跑起来，使用 GPU 进行加速。
4. 如果运行中遇到由于缺少 3D 渲染模型（如 MANO 模型）而导致的报错，请直接帮我魔改代码跳过可视化渲染，但保持核心的 3D 骨骼数据计算，直到实验能够正常输出显卡加速下的 Loss 或评测结果。

现在，请从【第一步】开始帮我执行，谢谢！